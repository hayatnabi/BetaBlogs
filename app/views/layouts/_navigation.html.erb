<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <div class="d-flex align-items-center">
      <img src="https://cdn-icons-png.flaticon.com/128/9852/9852209.png" alt="Logo" width="30" height="24" class="me-2">
      <%= link_to "betablogs.pk", root_path, class: "navbar-brand", id: "logo" %>
    </div>
    <div class="ms-auto d-flex align-items-center gap-3">
      <% if logged_in? %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Hey, <%= current_user.username %><%= " (Admin)" if current_user.admin? %>
            </a>
            <ul class="dropdown-menu">
              <%= link_to "Create Blog", new_article_path, class: "dropdown-item" %>
              <%= link_to "Open Profile", user_path(current_user), class: "dropdown-item" %>
              <li><hr class="dropdown-divider"></li>
              <%= button_to "Delete Profile", user_path(current_user), class: "dropdown-item text-danger", method: :delete, data: { confirm: "All of your data will be removed. Are you sure you want to delete your profile?"} %>
            </ul>
          </li>
        </ul>
        <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-outline-danger" %>
      <% end %>
    </div>
  </div>
</nav>
